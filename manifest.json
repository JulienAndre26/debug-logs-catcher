{
  "manifest_version": 3,
  "name": "Debug Logs Catcher",
  "version": "0.1.0",
  "description": "Capture WebSocket frames, console logs, and network requests for any tab via chrome.debugger. Export as JSON.",
  "permissions": ["debugger", "storage", "tabs", "webNavigation"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "service_worker.js"
  },
  "action": {
    "default_popup": "popup.html",
    "default_title": "Debug Logs Catcher"
  }
}